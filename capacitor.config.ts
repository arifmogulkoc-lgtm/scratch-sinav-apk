import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scratchquiz.app',
  appName: 'Scratch Sınav',
  webDir: 'dist/public'
};

export default config;
